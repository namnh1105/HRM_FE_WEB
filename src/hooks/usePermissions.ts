import { useAppSelector } from '@/store/hooks'; export function usePermissions() { const permissions = useAppSelector((state) => state.auth.permissions) || []; const hasPermission = (code: string) => permissions.includes(code); const hasAnyPermission = (codes: string[]) => codes.some((code) => permissions.includes(code)); const hasAllPermissions = (codes: string[]) => codes.every((code) => permissions.includes(code)); return { permissions, hasPermission, hasAnyPermission, hasAllPermissions }; }
